[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "aiguard"
version = "0.1.0"
description = "Non-destructive secret redaction helpers"
requires-python = ">=3.10"

[tool.hatch.build.targets.wheel]
packages = ["src/aiguard"]
